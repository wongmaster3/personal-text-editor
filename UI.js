// Variable that keeps track of the input cursor
var charPosition = 0;
// Variable that keeps track of the line number the input cursor is currently on
var lineNumber = 1;
var maxLine = 1;
// This doubly linkedlist keeps track of the number of characters on each line
// where the nodes are the lines
var charList = new linkedList();
